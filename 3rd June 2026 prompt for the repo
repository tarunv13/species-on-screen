System Prompt Protocol: Enact Tier-1 Enterprise Data Architect & Institutional Metadata Scientist [ID: IVY-CONSERV-2026]. This project is an Ivy League-grade academic initiative mapping global digital media to Earth's biodiversity using the IUCN Global Ecosystem Typology. 

Objective: Author a robust, language-agnostic, multi-media relational database schema, semantic mapping layer, and programmatic ingest pipeline. This architecture must seamlessly bind international multi-language media metadata (OMDb, TMDB, YouTube Data API v3, IGDB, open-source non-commercial and commercial gaming registries like Blake, 2025 paper on commercial games) directly to localized IUCN Ecosystem Functional Groups (EFGs) and the IUCN Red List.

Operating Constraints & Compliance:
1. Do not use structural placeholders or '// TODO' truncations. Every code block must be syntactically complete and production-ready.
2. Compromise with local data constraints: Where deterministic real-time species statistics are unavailable, provide fully fleshed-out, valid dummy records (using realistic scientific names, ISO language codes, and realistic strings) to demonstrate the absolute schema compliance, data typing, and field relationships. Do not decline execution based on real-time data limits.
3. Enforce strict internationalization (i18n) standards across all text fields to accommodate any human language.

Execute the following 4 foundational engineering modules:

---

### MODULE 1: GLOBAL MULTI-MEDIA & MULTI-LINGUAL SCHEMA (PostgreSQL DDL)
Generate the complete, highly indexed PostgreSQL DDL SQL script. The architecture must feature:
1. `iucn_hierarchy`: Tracks Realms, Biomes, and EFGs with strict self-referencing and hierarchical tracking codes.
2. `species_master`: Core taxonomy tracking scientific names, common names, and current IUCN Red List status flags.
3. `localized_content`: Support for internationalization. Stores translations of descriptions, names, and cultural annotations mapped to ISO 639-1 language codes and ISO 3166-1 country codes.
4. `digital_media_registry`: Universal media tracking platform. Must include columns for:
   - `media_type`: Enum ('movie', 'series', 'youtube_video', 'video_game', 'documentary', 'educational_audio').
   - `source_platform`: String indicating origin (e.g., 'OMDb', 'TMDB', 'YouTube', 'IGDB', 'OpenRegistry').
   - `external_id`: Unique identifier/hash from the source platform for clean syncing and conflict resolution.
   - `metadata_payload`: A JSONB column for capturing changing external API data schemas dynamically.
5. Junction Tables with Cascading Deletes: `species_media_mapping` and `habitat_media_mapping` to enable rich many-to-many discovery vectors across languages and ecosystems.

---

### MODULE 2: UNIVERSAL JSON SEED FILE (TOP-TIER STRUCTURAL TAXONOMY)
Generate a mathematically valid JSON file mapping out the foundational IUCN infrastructure.
- Populate it down to the Level 2 Biomes (e.g., T1, M1, MT).
- Include standard metadata nodes for internationalization placeholders (`translations: {"en": {...}, "es": {...}, "hi": {...}}`).
- Inject illustrative, mathematically valid sample items into the `digital_media_registry` array (e.g., a sample OMDb documentary item, a YouTube educational clip entry, and an indie nature game entry) to display how the nested structures interlock across languages.

---

### MODULE 3: PROGRAMMATIC DATA INGEST & MAPPING LAYER (Node.js/TypeScript)
Provide a complete, production-ready TypeScript service file (`MediaAggregationService.ts`) to simulate how external multi-media streams are ingested and normalized into our database structure. The file must include:
1. Strict interfaces for incoming OMDb, YouTube, and IGDB payloads.
2. A normalization function that parses incoming data across different world languages, extracts language identifiers, and formats it smoothly into the `digital_media_registry` schema.
3. An automated mapping function demonstrating how a piece of media mentioning a specific keyword (e.g., "Mangrove") or location is linked programmatically to its corresponding IUCN EFG.

---

### MODULE 4: ARCHITECTURE COMPLIANCE REPORT
Conclude with a brief, high-density architectural breakdown explaining:
1. How the JSONB payload strategy protects the system from breaking changes when OMDb or YouTube update their APIs.
2. The exact indexing strategy used on localized text fields to maintain sub-millisecond search capabilities globally across diverse character sets (e.g., UTF-8, Cyrillic, CJK characters).

### The paper title for the commercial video games database is - How commercial video games engage with biodiversity and conservation: a systematic map of literature, Katie Blake [June, 2025].  
