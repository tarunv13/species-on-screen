# Eco-Cinema Observatory — Project Status

**HEAD:** `feat/exploration-prototypes-and-data-pipelines` · build green · `observatory-v1.0` at e61e898 · observatory-v2.0 release prepared at 72caec7 (PASS WITH WARNINGS, 0 blockers; tag recommended, see `.agents/sessions/2026-07-02-observatory-v2.0-release.md`) · post-v2.0: M21 closes the review's sole Major warning (EPR homepage integration) · M22 establishes the before-PhD open-science governance foundation (Apache-2.0 code, CC-BY-4.0 docs/data, CITATION.cff, EML rights, sensitive-locality policy) · v3.0: M23 lands the Place Manifest (ADR-001, Phase 0); M24 (Research) / M25 (Atlas) / M26 (Homepage) complete the consumer migration (Phase 1A–1C); **M27 retires `public/dwca/index.json` — the Place Manifest is now the single canonical source of truth (no duplicated metadata); output parity preserved; Cinematic surfaces remain pure.** · **Embodiment Phase opens: M28 lays the founding specification for the Ecological Knowledge Environment as the reference implementation for evidence-preserving ecological communication, and ships the reference validator's first behavior (`check-bindings`, claim-level traceability certification over the four archives).** · **M29 gives the EPR atlas field record a diagnostic hydrothermal-vent backdrop (`abyssal` recipe + `smokers` signature in `src/prototypes/biome-backdrop.js`), closing the mismatch where `atlas/epr-vents.html` rendered a sunlit painterly landscape behind a lightless chemosynthetic vent field; Cinematic surfaces remain untouched.** · **M30 adds `check-dwca-xml` — a dependency-free well-formedness gate on the four archives' `meta.xml`/`eml.xml`, wired into `prebuild` (build invariant) and `verify` — closing a previously-undetected corruption risk (the EML template interpolates place name/locality into XML text with no escaping).** · **M31 closes the EPR DwC-A reference audit (backlog item 1): 7 of 9 flagged citations in `public/dwca/epr-vents/CREDITS.md` are now DOI-anchored (independently confirmed against CrossRef, one title correction found), a book correctly needs none, and one (Bates et al. 2005) is left honestly unresolved with a documented ambiguity rather than a guessed DOI; `CREDITS.md` is a live visitor-facing page linked from `atlas/epr-vents.html`.** · **M32 continues the sole remaining thread of the audit (backlog item 4, Bates et al. 2005): re-examined the citation against both candidate papers' full CrossRef records plus a newly-surfaced third lead (a 2006 Bates PhD dissertation spanning both themes); neither candidate matches both the cited byline and subject, so the DOI remains explicitly unresolved — now with stronger, better-documented reasoning — rather than forced.** · **Observatory v2 opens: the frozen five-layer design cascade (Interaction Grammar → Architecture Decisions D1–D10 → capability/workflow → roadmap M33–M40) is now transcribed into the repository as the canonical `OBSERVATORY_V2_IMPLEMENTATION.md` (roadmap + implementation ledger), no longer conversation-only. M33 (D7) makes non-resolution a first-class terminal state on the evidential surface: the evidence ledger (`scripts/build-evidence.mjs`) now renders reach in three honest states (traceable / open / gap) derived from the validator's own L1+L2 verdicts — replacing the two-state pass/fail that over-claimed "38/38 traceable" while hiding that no binding source resolves to a persistent identifier; a new pure classifier (`scripts/evidence-reach.mjs`) is unit-tested and wired into `verify` (now 9 checks).** · **M34 (D6) completes the reach-not-truth badge grammar on that same surface: every claim now carries a badge that names how far its warrant *reaches* (`REACHES EVIDENCE` / `REACHES A SOURCE` / `REACH INCOMPLETE`) — no pass/fail, error, or check/cross language (unit-enforced) — beside the validator's reason codes shown *verbatim* as `<code>` chips (e.g. `SOURCE_UNRESOLVABLE`), never paraphrased; the `open` state keeps its calm D7 first-class note, and the ledger index now names all three reach-states.** · **M36 (D4) lands the *follow* primitive on the analytical surface: the atlas field-record interaction web is now a set of RO-typed, laterally-followable edges — each actor an addressable node, each edge naming its OBO Relations Ontology relation (linked to the controlled IRI the validator checks) and letting you follow it to the actor it links at the *same* depth (an in-page fragment, never a press-in/step-back); a new pure `src/atlas/interaction-web.js` model is unit-tested (`verify` now 10 checks) and real-data-verified across all four archives (100% of edges RO-typed, all IRIs valid, all follow-targets lateral); cinematic surfaces untouched.** · **M35 (D5) lands the *interrogate* primitive on the evidential surface: each claim in the evidence ledger carries a native `<details>` reveal that opens in place (no navigation, zero JS) and discloses the full evidence chain — verbatim validator verdict, reason codes, source, and each occurrence's backbone/as-of/pinned status — inlined at build time straight from `check-bindings.js --json` via a new pure `scripts/evidence-interrogate.mjs`; a headless cross-check confirmed all 190 inlined fields match the validator exactly (`verify` now 11 checks). The verbatim verdict now lives in the interrogation reveal, closing the M34-review gap without pass/fail language in the badge.** · **M37 (D3/D9) turns two Observatory v2 invariants into enforced build gates: `scripts/check-cinematic-grammar.js` (dependency-free, wired into `prebuild` and `verify`) fails the build if the cinematic runtime (JS + CSS + place shells, comments stripped) hosts any depth affordance — a cross-depth import/navigation, an evidential/analytical logic identifier, an `interrogate` marker, or a cross-document view-transition (D3, the affordance-sink) — or if any depth-transition subject surface (research article, atlas field-record, evidence ledger) is missing the shared `eke-subject` view-transition morph or declares a foreign one (D9, the single-subject invariant); negative-tested (19 fixture checks + a live injection caught then reverted); `verify` now 12 checks.** · **M38 (D10) consolidates the grammar into a *single* composite gate (`scripts/check-grammar.js`, `npm run check-grammar`) that rejects any change violating any of the **four** constraints — affordance placement (D3) · subject morph (D9) · subject invariant (D1, `eke-subject` is the only view-transition-name anywhere) · depth discreteness (D2, no programmatic cross-depth navigation) — by *composing* M37's pure predicates (M37 untouched) plus two new ones in `scripts/grammar-constraints.mjs`; wired into `prebuild`, `verify`, and an explicit CI step in `.github/workflows/verify.yml`; negative-tested per constraint (9 checks) + live-injection-verified; `verify` now 13 checks.** · **M39 (D1) makes the held *subject* URL-addressable: a new shared, dependency-free `src/subject.js` (`subjectIdFromSearch`/`resolveSubject`/`heldSubject`/`withSubject`) + a typed manifest `resolveSubject` let `?subject=<canonical placeId>` resolve to the same subject on every surface via the one manifest resolver (no per-surface bespoke mapping) — e.g. `?subject=east-pacific-rise` → the `epr-vents` cinematic/atlas/dwca slug and the EPR research slug everywhere; the atlas field record resolves `?subject=` to establish its place, and the atlas/research/evidence surfaces carry `?subject=` on their cross-surface links; unknown/absent degrades gracefully; **no cinematic file touched** (purity + the M38 grammar gate intact); 37-check unit test (`verify` now 14 checks).** · **M40 (D8) closes the roadmap: interrogation state now lives in the URL *fragment* — each evidence-ledger claim is `id="claim-N"` and a small progressive-enhancement script opens the claim addressed by `#claim-N` on load and on back/forward, writing the fragment when a claim is opened (a history entry); with the subject in `?subject=` (M39), follow in `#fr-node` (M36), and cross-depth `<a href>` navigations, the four primitives each produce a history entry, so back/forward retraces the reasoning path and a copied URL restores subject + depth + interrogation; pure fragment↔claim mapping in `scripts/interrogation-url.mjs` (15-check test); M35's static evidence content is unregressed (script only syncs open/close ↔ URL); `verify` now 15 checks. **The Observatory v2 roadmap M33–M40 is complete.** · **R1 (Browser/visual QA of the realized grammar) is CLOSED (2026-07-05)** — the two remaining items (the `eke-subject` view-transition morph across research→evidential and atlas→evidential descents with live `?subject=` resolution; the M28 final transition easing/timing polish) were driven against `dist/` in a real Chromium browser on the East Pacific Rise place: both descent paths completed cleanly, the place title carried through unchanged on each transition, `?subject=east-pacific-rise` resolved correctly on both paths, and no console errors were observed; the cinematic-purity grep (`grep -rl view-transition dist/assets/*.css`, no `places-*.css`) and both grammar gates (`check-grammar`, `check-cinematic-grammar`) independently confirmed purity. No implementation defect was found; no code changed. `npm run verify` (17 checks) and `npm run build` both green. See `.agents/sessions/2026-07-05-r1-browser-qa-closure.md`. **R2 (merge to `main`) is now formally unblocked.**** · **The Thought System is introduced (TS0+TS1, 2026-07-07):** a new institutional-memory layer at `.agents/thoughts/` captures durable *pre-resolution* reasoning — the working-memory tier upstream of ADRs and milestones that previously survived only in conversation (the loss vector the M33 entry records). A Chief Architect ADR (`.agents/decisions/2026-07-07-thought-system.md`) ratifies V1.0 (flat directory; `domain` list + `origin` provenance; five-state lifecycle where promotion freezes read-only and never shortens; Thesis/Reasoning/Evolution/Cross-references sections), amends `.agents/AI-OS.md` (a Thoughts memory layer + a session-close Thought Review step) and `PROJECT_OPERATING_MANUAL.md` §1, and ships the static scaffold (`_TEMPLATE.md`, hand-maintained `INDEX.md`, and one worked example Thought explaining the system itself). **Automation is intentionally deferred:** the validator, derived-index generator, scaffold script, `package.json`/`verify`/CI wiring (roadmap TS2–TS4) are NOT built — they require real repository usage before the operating principle ("modified only when real work demonstrates a deficiency") justifies them; the `confidence` field is rejected permanently (it is the pass/fail summary register M34 forbids). Docs-only; no production surface, build gate, or shipped artifact touched. `npm run verify` (17 checks) and `npm run build` both green.

---

## Production state

| Surface | Pages |
|---|---|
| Cinematic | `index.html`, `places/sundarbans.html`, `places/crossing.html`, `places/epr-vents.html` |
| Atlas | `atlas/index.html`, `atlas/sundarbans.html`, `atlas/amazon-varzea.html`, `atlas/crossing.html`, `atlas/coral-triangle.html`, `atlas/epr-vents.html` |
| Research | `notes/` × 13 (Sundarbans, Coral Triangle, Amazon várzea carry cross-surface nav) |

Navigation graph complete. M1–M17 + R1 shipped. Coral Triangle certified as Observatory reference implementation. Amazon várzea DwC-A orphan resolved: 3-surface experience live. **13 of 13 narratives are `verified`**; 0 remain `draft`. East Pacific Rise now has the full three-surface experience: cinematic (`places/epr-vents.html`, UX-reviewed), research (`notes/east-pacific-rise-tubeworm-chemosynthesis.html`), and atlas field record (`atlas/epr-vents.html`, M20) reading its DwC-A (`public/dwca/epr-vents/`, M19). `public/dwca/index.json` lists 4 archives. All three cinematic places now have a homepage entry point — the planetary homepage carries three place captions (Sundarbans, Coral Triangle, East Pacific Rise), so EPR is discoverable from the primary entry (M21). See `git log` and `.agents/sessions/` for execution history.

---

## Active blockers

None.

---

## Backlog

1. **Amazon várzea cinematic surface** — `places/amazon-varzea.html`, deferred; would complete existing 3-surface infrastructure
2. **Open-science roadmap (during/after PhD, per approved Option-B strategy; M22 laid the before-PhD foundation)** — Zenodo integration + concept/version DOIs (into `CITATION.cff` + EML `packageId`); `codemeta.json`; schema.org `Dataset` JSON-LD on research pages; CI validation of internal links; Software Heritage + periodic Internet Archive snapshots; GloBI contribution of the interaction records; DwC-A reproducibility parity. Author full-name/ORCID completion in `CITATION.cff`. GBIF registration only if the archives are reframed (checklist/interaction/sampling-event) — decision papers 2026-07-02. (The "EML lint" component is done — see M30, `check-dwca-xml`.)
3. **Cinematic coherence follow-ups (from M18 review)** — (a) Sundarbans↔canvas interaction-model divergence (click/GSAP-timeline vs scroll/Canvas2D) — the Observatory's central coherence tension. **No longer blocked by governance**: the Chief Architect ruling has been made and recorded (`.agents/decisions/2026-07-05-wp8-interaction-model-adr.md`, RATIFIED 2026-07-05) — Option A (converge on scroll-scrubbed Canvas2D — the interaction model, not the rendering technology; see the ADR's 2026-07-05 amendment: Sundarbans' DOM/CSS/SVG rendering is not required to change). **The retrofit is implemented and WP8 is certified PASS WITH WARNINGS** (ADR Amendment 3, 2026-07-05) — one open warning remains, see backlog item 5. **Amendment 4 (2026-07-19) partially reopens this**: an accepted design investigation found the Sundarbans Descent's direct scroll-position coupling violates Principle III's timed-not-improvised contract; the driver is re-implemented as a time-governed progression with a perceptual floor (scroll conveys intent; the timeline advances at no more than its authored rate). The reopened item closes only when the implementation passes Claude Design's Descent Approval Specification in browser verification — **closed 2026-07-20: Claude Design returned PASS**; the governed driver (monotone progress per C2, arrival-as-event per C4, dev-only `__descent` instrumentation) is the certified implementation. (b) Sundarbans `.vignette` (opacity 0.55) — **resolved**: removed per Article V's vignette prohibition (WP3). (c) Cross-surface type-stack drift — **resolved**: Crossing and East Pacific Rise now lead with "Iowan Old Style", matching Sundarbans (WP6). (d) EPR camera `ay: H*0.50` vs The Crossing's `H*0.60` arrival framing — **reviewed**: an intentional per-scene compositional difference, not unresolved drift (WP7); values unchanged. (e) Screen-reader exposure of editorial captions (currently `aria-hidden`); pending the forthcoming accessibility doctrine. (f) EPR/Crossing per-frame performance measurement (Principle XVI / Article XV).
4. **Bates et al. (2005) citation ambiguity** — `public/dwca/epr-vents/CREDITS.md` cites "Bates, A.E. et al. (2005). Grazing and microhabitat of the vent limpet *Lepetodrilus*" with no DOI. The M31 audit found the year points to a thermal-habitat paper (not grazing); the M32 re-audit deepened this with a third lead — a 2006 Bates PhD dissertation spanning both themes — and confirmed via direct CrossRef resolution that neither of the two candidate papers matches both the byline ("et al.") and the subject ("grazing"). Needs a Research Curator to inspect the actual papers/thesis directly (not just abstracts) and decide, or confirm the citation was intentionally a synthesis of general knowledge rather than one paper.
5. **Sundarbans `mist-rising` reduced-motion warning (WP8)** — under `prefers-reduced-motion: reduce`, `.mist-rising` is fully suppressed (`transform: none`) rather than magnitude-reduced, because the alternative was empirically confirmed to overshoot its resting position. The accepted composition audit found this does not preserve the layer's documentary meaning ("Surrender of Frame" — mist rising and passing over the threshold). Ruled an explicit open warning, not an accepted trade-off (`.agents/decisions/2026-07-05-wp8-interaction-model-adr.md`, Amendment 3, 2026-07-05) — honestly disclosed and open to future work, not blocking WP8's certification.
6. **Documentation governance integration** — a four-document governance proposal (`WORK_QUEUE.md`, `ROADMAP.md`, `OWNERSHIP_MATRIX.md`, `.docs/Repository Publishing Guide.md`) drafted in a prior session was found, on review, to duplicate mechanisms the repository already has. The Chief Architect ruling has been made and recorded (`.agents/decisions/2026-07-07-documentation-governance-integration.md`, RATIFIED 2026-07-07): the proposal is rejected as a parallel governance layer, and its content is integrated as follows. (a) **Done**: this Backlog entry tracks the meta-decision (reject the four-document layer); the WQ-1–WQ-9 documentation-correction items the rejected `WORK_QUEUE.md` itemized (e.g. a "one cinematic place"/narrative-count drift across `PROJECT_STATE.md` and `private-book/`, a missing `atlas/` bundling clause) are **not yet independently verified or itemized here** — they depend on an external Canonical Publishing Map not re-audited in this pass; open for a future session, not fabricated now. (b) **Done**: the reusable documentation-canonicalization procedure is absorbed into `PROJECT_OPERATING_MANUAL.md` §6. (c) Ownership and role authority are unchanged, per `.agents/AI-OS.md`; this entry defines no new roles. (d) **Done**: the four proposed documents and the `.docs/` directory have been removed.

---

## Completed milestones

- **Thought System TS0+TS1** (2026-07-07) — **A new institutional-memory layer for pre-resolution reasoning, introduced under a Chief Architect ruling and limited to its two non-automation milestones.** Closes a real, recurring deficiency: the repository has a home for every *resolved* form of work (doctrine, ADRs, session diaries, milestone state, the book) but none for the reasoning that spans sessions *before* it becomes a ruling or a milestone — which today survives only in conversation and is lost when the conversation ends (the exact loss the M33 entry records for the Observatory v2 design cascade). **TS0 (governance):** `.agents/decisions/2026-07-07-thought-system.md` ratifies the Thought System V1.0 and formally amends two frozen governance documents — `.agents/AI-OS.md` gains a **Thoughts** row in the Institutional Memory Architecture table and a session-close **Thought Review** step (step 11: did the work generate durable thinking? evolve an existing Thought if one owns it, else create one, never duplicate), and `PROJECT_OPERATING_MANUAL.md` §1 gains a matching Thoughts row. **TS1 (static scaffold, usable by hand):** `.agents/thoughts/` with `_TEMPLATE.md` (five-state lifecycle Working→Under Review→Promoted→Archived→Superseded; `domain` list over {production, editorial, research, architecture}; required `origin` provenance; Thesis/Reasoning/Evolution/Cross-references), a hand-maintained `INDEX.md`, and one worked example Thought (`2026-07-07-thought-system-v1.md`) that explains the system by being it — a Promoted (frozen, read-only) Thought whose Evolution log records the four real design revisions of this session, each anchored to a resolving repository artifact, pointing at its own ratifying ADR. **Design invariants:** promotion *freezes* a Thought read-only and preserves its full reasoning as reasoning-of-record — it never shortens it (provenance is not duplication; the M27 single-source rule holds because current-decision and reasoning-of-record are different facts with one home each). **Intentionally NOT implemented (roadmap TS2–TS4):** no validator, no derived-index generator, no scaffold script, no `package.json`/`verify`/CI change — deferred until real usage of the folder demonstrates the deficiency, per the AI-OS operating principle and its "Never create speculatively" Appendix rule. The `confidence` field is rejected permanently (it is the pass/fail summary-judgment register the evidence ledger was engineered to forbid — M34's forbidden-term test, M33/D7's "open question, not a failure"). Dormant, an Evidence section, and a Thought-Review invalidation question are accepted-in-principle but deferred, to be recorded as the system's own first Thoughts once it is in use. Docs-only; `src/`, `prebuild`, `build`, and every shipped artifact untouched. `npm run verify` (17 checks) and `npm run build` both green. Files: `.agents/decisions/2026-07-07-thought-system.md`, `.agents/thoughts/_TEMPLATE.md`, `.agents/thoughts/INDEX.md`, `.agents/thoughts/2026-07-07-thought-system-v1.md`, `.agents/AI-OS.md`, `PROJECT_OPERATING_MANUAL.md`, `PROJECT_STATUS.md`, `.agents/sessions/2026-07-07-thought-system-ts0-ts1.md`. One clean commit.
- **R1 browser QA closure** (2026-07-05) — **R1 (Browser/visual QA of the realized grammar) is formally CLOSED.** The two items the M36 browser-QA session had left open — the `eke-subject` view-transition morph across the research→evidential and atlas→evidential descents (D1/D9), including the live `?subject=` resolution; and the M28 carryover (final transition easing/timing polish, `.agents/HANDOFF-eke-completion.md` item 1) — were driven against the built `dist/` in a real Chromium browser on the East Pacific Rise place. **Findings:** both descent paths (`notes/east-pacific-rise-tubeworm-chemosynthesis.html` → evidence ledger; `atlas/epr-vents.html` → evidence ledger) navigate cleanly via the View Transitions mechanism; `view-transition-name: eke-subject` is present and identical on the research-article `h1`, the atlas field-record masthead `h1`, and the evidence-ledger `h1`; the place title carries through unchanged across each transition; `?subject=east-pacific-rise` is correctly carried and resolved on both paths; no console errors on either path. **Cinematic purity independently reconfirmed:** `grep -rl view-transition dist/assets/*.css` matches only `render-narrative`/`field-record`/`atlas-index` (no `places-*.css`); `npm run check-grammar` and `npm run check-cinematic-grammar` both report all constraints holding on the real build. **Outcome: PASS on all three evaluated items (M28, M37/M39 morph, cinematic purity) — no implementation defect found, no code changed.** `npm run verify` (17 checks) and `npm run build` both green, re-confirmed at closure. Combined with the already-closed M36/M40 browser QA and the already-fixed async cold deep-link restore, **every R1 acceptance item is now satisfied — R1 is closed.** **R2 (merge `feat/exploration-prototypes-and-data-pipelines` → `main`) is now formally unblocked**, contingent only on the standing `verify`/`build` gate holding at merge time. Docs-only; no implementation code touched. Files: `PROJECT_STATUS.md`, `OBSERVATORY_V2_IMPLEMENTATION.md`, session diary. One clean commit. Session diary: `.agents/sessions/2026-07-05-r1-browser-qa-closure.md`.
- **Follow deep-link restore** (2026-07-05) — **Completes the D8 restorability claim for the *follow* primitive (D4/D8) on the async field record.** The M36 browser QA (below) surfaced a genuine gap between the roadmap's written acceptance and the repository: M40/D8 states the follow primitive is *"restorable via native `:target` scroll"* and *"a copied URL restores subject + depth,"* but a **cold deep-link** `atlas/<place>.html#fr-node-<id>` did **not** restore the followed node — the field record builds its follow web **asynchronously** after the archive fetch, so the target is absent at the browser's load-time fragment resolution and native `:target`/scroll never fire (warm follow + Back/Forward were already fine). **Fix (minimal, reuse of the M40 pattern):** a new dependency-free pure mapping **`src/atlas/follow-url.js`** (`FOLLOW_PREFIX`/`followDomId`/`followDomIdFromHash`) — the single fragment↔dom-id definition, the same shape as M40's `interrogation-url.mjs` for `#claim-N` — is now shared by the follow **click handler** (routed through it) and a new **on-load restore** in `src/atlas/field-record.js`: after `buildSources()`, if `location.hash` addresses a follow node, the renderer scrolls it into view and applies the same `is-followed` pulse + a11y focus a click gives. **Verification:** new `scripts/follow-url.test.mjs` (15 checks — round-trip; `#claim-N`/subject/empty fragments ignored; percent-decoding), wired into `verify` as `test:follow-url` (now **12 unit tests**, green); `npm run build` green; **browser-verified against `dist/`** — a cold `#fr-node-EPR-VENTS:OCC:5` now scrolls the node into view (top ≈ 24px, page scrolled) with the highlight pulse and focus, and the warm follow click (hash + one history entry + `:target` + pulse + focus) is unregressed. **Scope held:** no cinematic file touched (grammar gate green — the restore uses `getElementById`/`scrollIntoView`, no cross-depth navigation); no redesign; M40's `#claim-N` and M36's follow action untouched. Files: `src/atlas/follow-url.js`, `src/atlas/field-record.js`, `scripts/follow-url.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, `PROJECT_OPERATING_MANUAL.md`, `.agents/sessions/2026-07-05-follow-deeplink-restore.md`. One clean commit.
- **M36 browser QA** (2026-07-05) — **R1 browser-behavioral acceptance for the *follow* primitive (D4).** Drove the built `dist/` (`vite preview`) on the EPR atlas field record in a real browser and confirmed every M36 in-browser behavior the milestone had only verified structurally: the follow web renders as **20 same-depth lateral edges** (every `#fr-node-<id>` target resolves to a node on the same page); activating a `.fr-follow` anchor sets `#fr-node-<id>`, matches `:target` (node background `rgba(47,125,107,0.12)` = `--fr-accent-soft`), runs the `fr-follow-pulse` animation (`running`), moves a11y focus to the `<section tabindex="-1">`, and adds **exactly one** history entry; real browser **Back/Forward retrace** re-resolves `:target` (OCC:3 ⇄ OCC:6); and the built stylesheet suppresses the pulse under `@media (prefers-reduced-motion: reduce)` (`.fr-node.is-followed { animation-name: none }`). **Existing code satisfied every M36 acceptance criterion — no code changed** (same outcome as the M40 browser-QA pass). **One honest limitation recorded, not fixed:** a **cold deep-link** load of `atlas/<place>.html#fr-node-<id>` does not restore `:target`/scroll, because the field record builds its follow web **async** after a fetch, so the target is absent at load-time fragment resolution and `:target` is never re-resolved (warm follow + Back/Forward are unaffected). This is an **M40 restorability edge on an async surface, not an M36 defect** — M40 is closed/audit-accepted and its browser QA verified the `#claim-N` restore on the static evidence ledger; deferred to R1 (fixing it would reopen a closed milestone). Files: `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, `.agents/sessions/2026-07-05-m36-browser-qa.md`. Docs-only. **R1's M36 item is now closed;** remaining R1 = M37/M39 view-transition morph, M28 easing polish, and the recorded async cold-restore edge.
- **M40** (2026-07-04) — **Observatory v2, final roadmap milestone: history-as-trace + interrogation state in the URL (D8).** Makes browser **history the primitive string** and carries **interrogation state in the URL fragment**. **Interrogation in the fragment (the active work):** each interrogation `<details>` on the evidence ledger now has `id="claim-N"` (1-based, stable per render, collision-free), and a small **self-contained progressive-enhancement `<script>`** in the generated ledger opens the claim addressed by `#claim-N` on load and on `hashchange` (back/forward), writes the fragment via `location.hash` when a claim is opened (a **history entry**, so opening an interrogation is retraceable), and drops the fragment on manual close. The pure fragment↔claim mapping lives in a new dependency-free **`scripts/interrogation-url.mjs`** (`CLAIM_PREFIX`/`claimDomId`/`domIdFromHash`) — the single definition used to stamp the DOM ids at build time and (inlined) drive the script; a non-interrogation fragment (M36's `#fr-node-…` follow target, or a `?subject=` query) is ignored. **History as the primitive string (holds for all four primitives without changing the others):** press-in / step-back are cross-depth `<a href>` document navigations (history entries, carrying `?subject=` per M39); **follow** is M36's `#fr-node-<id>` hash (a history entry, restorable via native `:target` scroll); **interrogate** now sets `#claim-N` (a history entry) — so back/forward retraces the reasoning path. **Shared, restorable URL model (consistent with D1/M39):** the subject stays in the query (`?subject=`), the interrogation in the fragment (`#claim-N`); they **coexist** (`withSubject('…#claim-3', id)` → `…?subject=id#claim-3`), so a copied URL `evidence/<slug>.html?subject=<placeId>#claim-N` restores **subject** (query, carried onward) + **depth** (the page = evidential) + **interrogation** (the fragment opens claim N). **M35 not regressed:** the evidence chain stays **static/inlined** — the script only manages open/close ↔ URL, never the evidence content — and without JS the `<details>` still work manually (progressive enhancement); the M35 review had explicitly deferred URL state to D8/M40. **Do not touch other milestones:** M36's follow and the cross-depth links were left untouched (they already produce history + restore); cinematic untouched; the M38 grammar gate stays green (the inline script uses `location.hash`/`history.replaceState` with no cross-depth string — not a programmatic cross-depth navigation). **Verification:** new `scripts/interrogation-url.test.mjs` (15 checks — round-trip; non-interrogation fragments ignored), wired into `verify` as `test:interrogation-url` (now 15 checks, green); `npm run build` green; the built ledger stamps 10 `id="claim-N"` + ships the restore script; the **extracted inline script passes `node --check`** (valid JS; the escaped `^claim-\d+$` regex renders correctly); the index page (no claims) carries no script; all verdicts/occurrences still present (M35 intact). Files: `scripts/interrogation-url.mjs`, `scripts/build-evidence.mjs`, `scripts/interrogation-url.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. **The Observatory v2 roadmap (M33–M40) is complete** — the Interaction Grammar (subject invariant + four primitives + traceability-not-truth) is now realized and enforced across the evidential, analytical, and experiential surfaces.
- **M39** (2026-07-04) — **Observatory v2, roadmap milestone: subject URL-addressability across surfaces (D1).** Makes the held **subject a URL-addressable manifest id** — `?subject=<canonical placeId>` — that resolves to the **same subject on every surface** via **one shared resolver**, no per-surface bespoke mapping. New dependency-free **`src/subject.js`** is the single mechanism: `subjectIdFromSearch` (read `?subject=`), `resolveSubject(places, id)` (→ place by canonical `placeId`), `heldSubject(places, search, fallbackId)` (resolved subject or graceful fallback to the surface's own identity), `withSubject(href, id)` (carry the id across a **declarative** navigation — preserving existing query/fragment, no-op on a falsy id). The typed `resolveSubject(subjectId)` was added to the manifest (`place-manifest.ts`, ADR-001; the subject id is the canonical `placeId`, deliberately distinct from per-surface slugs — e.g. `east-pacific-rise` → cinematic/atlas/dwca slug `epr-vents`, research slug `east-pacific-rise-tubeworm-chemosynthesis`; `coral-triangle` → cinematic slug `crossing`). **Consumers wired (each derives its own local slug from the one resolved place):** `src/atlas/field-record.js` resolves `?subject=` to establish its `PLACE` (subject → field-record slug wins, else `?place=`/filename) **and** carries `?subject=` on all four nav links; `src/atlas/atlas.js` carries it on the detail-card bridges + discovery chips; `src/notes/render-narrative.js` carries it on the cross-surface links (the pure `surface-links.js` derivation + its test **unchanged** — the subject is layered on at render); `scripts/build-evidence.mjs` carries `?subject=<placeId>` on the evidence ledger's ascent links. **Cinematic purity preserved — no cinematic file touched:** `src/main.js` and `src/places/*` are byte-identical; the cinematic surface is an affordance-sink (M37/M38), so it hosts no subject-carrying cross-depth affordance — the place *is* the subject, and other surfaces' links *into* it carry `?subject=`. **Navigation parity preserved:** link **targets** are unchanged (the `?subject=` is additive). **Graceful degradation:** unknown/absent subject falls back to the surface identity; a place with no cinematic surface (amazon-várzea) simply carries no cinematic link. **The M38 grammar gate stays green** — the new links are declarative `<a href>`/element-href assignments (not programmatic `location`/`history` nav), so depth discreteness (D2) holds, and no cinematic depth affordance was added (D3). **Verification:** new `scripts/subject.test.mjs` (37 checks against the **real manifest** — one id resolves to one subject with every surface slug derivable; parsing; `withSubject` with existing query/fragment/no-double-add; graceful degradation), wired into `verify` as `test:subject` (now 14 checks, green); `npm run build` green; built ledgers confirmed to carry `?subject=<placeId>` with the correct per-surface slug on all four places; cinematic bundles confirmed pure (no `?subject=` cross-depth logic in any `dist/assets/places-*.js`). **Scope held:** URL-addressability (read + resolve + carry + graceful); per-page content-switching beyond the field record and persisting the subject through history are M40 (D8), not started. Files: `src/subject.js`, `cinematic-language/place-manifest.ts`, `src/atlas/field-record.js`, `src/atlas/atlas.js`, `src/notes/render-narrative.js`, `scripts/build-evidence.mjs`, `scripts/subject.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M39 per protocol; next is M40 (D8, history-as-trace + interrogation in URL — the last milestone).
- **M38** (2026-07-04) — **Observatory v2, roadmap milestone: composite grammar-rejection CI gate (D10).** Consolidates the Interaction Grammar into **one single gate** (`scripts/check-grammar.js`, `npm run check-grammar`) that rejects any change violating **any of the four constraints**, reported grouped by constraint: **affordance placement (D3)** · **subject morph (D9)** · **subject invariant (D1)** · **depth discreteness (D2)**. **Composition, not duplication:** the gate imports M37's pure predicates (`findCinematicJsAffordances`/`…CssTransitions`/`…HtmlCrossDepthLinks`/`findMissingSubjectMorph` from `cinematic-grammar.mjs`) and two new ones from a new **`scripts/grammar-constraints.mjs`** — `findForeignSubjectNames` (D1) and `findScrollDepthCrossing` (D2) — which themselves reuse M37's `stripJsComments`/`SUBJECT_MORPH`; no logic is re-implemented. **The two new constraints, operationalised:** **D1 (subject invariant)** — `eke-subject` is the **only** `view-transition-name` across production stylesheets (a foreign name is a competing subject; test fixtures under `scripts/*.test.mjs` are excluded from the scan); **D2 (depth discreteness)** — cross-depth movement must be a **declarative `<a href>`** (a user navigation → multi-document view transition), never a **programmatic `location`/`history` jump** that scroll or a timer could drive, so depth stays discrete and multi-document (`places/` — the experiential depth — is excluded, so the same-depth cinematic arrival is not flagged; a declarative `anchor.href = 'evidence/…'` is not flagged, only actual navigation). The composite exposes a pure `evaluateGrammar()` so it is unit-testable without file IO. **Wired as the single gate:** `check-grammar` **replaces** the standalone M37 gate in `prebuild` and `verify` (the D10 consolidation) and runs as an explicit **"Grammar gate (D10)"** step in `.github/workflows/verify.yml`. **M37 is untouched:** its three files are byte-identical and `test:cinematic-grammar` still runs — the composite merely reuses M37's predicates and supersedes its standalone gate in wiring. **Verification:** new `scripts/check-grammar.test.mjs` (9 checks — **each** of the four constraints violated in turn and rejected, plus a clean world, a declarative-anchor-not-a-crossing case, and a same-depth-arrival case), wired into `verify` as `test:grammar` (now 13 checks, green); the gate **passes on real code** (all four hold); **live injection** proved it catches a real violation (added `view-transition-name:rogue-subject` to `src/atlas/atlas.css` → exit 1 under "subject invariant (D1)" → reverted, `git` clean); `npm run build` green (`prebuild` runs the composite). Files: `scripts/grammar-constraints.mjs`, `scripts/check-grammar.js`, `scripts/check-grammar.test.mjs`, `package.json`, `.github/workflows/verify.yml`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M38 per protocol; next is M39 (D1, subject URL-addressability across surfaces — high blast radius, spans sessions), then M40.
- **M37** (2026-07-04) — **Observatory v2, roadmap milestone: cinematic-purity + subject-morph build gates (D3, D9).** Turns two grammar invariants — previously only checked by hand in prior milestones' review greps — into an **enforced, dependency-free build gate** (`scripts/check-cinematic-grammar.js`), wired into **both `prebuild` (a build invariant) and `verify`**. **D3 (affordance-sink / one-way bridge):** the gate fails if the **cinematic runtime** hosts any depth affordance — a cross-depth `import` (of an atlas/notes/evidence/validator module), a cross-depth navigation string (`atlas/`|`notes/`|`evidence/`), an evidential/analytical logic identifier (`interrogationChain` / `interactionWebModel` / `classifyReach` / `REACH_META` / `reasonCodesVerbatim`), an `interrogate` marker, a cross-document `@view-transition`/`eke-subject` in cinematic CSS, or a real cross-depth `<a>` in a place shell. Prose comments (JS block/line, HTML `<!-- -->`) are **stripped first**, so the shells' wiring-documentation references to `notes/` (and the `sundarbans.js` header comment) do not false-trigger. **D9 (single-subject invariant):** the gate fails if any of the three depth-transition **subject surfaces** — research article (`src/notes/research-article.css`), atlas field-record (`src/atlas/field-record.css`), evidence ledger (`scripts/build-evidence.mjs` CSS) — is missing the `@view-transition` opt-in or the `view-transition-name: eke-subject` morph, or declares any **other** view-transition-name. The pure predicates live in a new dependency-free **`scripts/cinematic-grammar.mjs`** (stripping + detection), so they are unit-testable; the CLI reads the real surfaces. **Do not touch other milestones:** M37 only **adds** files + `package.json` wiring — no cinematic/atlas/evidence source was modified. **Scope decision:** the gate enforces D3 on the cinematic **source** (the authoritative input the bundle is built from — deterministic, runs before the build, and vite injects no affordances) rather than grepping the post-build bundle; `index.html` (the hub) is exempt from the shell cross-depth-link check because its caption anchors are intentional no-JS fallbacks the runtime intercepts (its runtime, `src/main.js`, is still gated and holds no cross-depth string); the atlas overview (`atlas.css`) has the calm cross-fade but no held subject, so it is correctly **not** a subject surface. **Verification:** new `scripts/check-cinematic-grammar.test.mjs` (19 negative + positive checks — every failure mode discriminated, every clean shape passes), wired into `verify` (now 12 checks, green); the gate **passes on real code** (D3 across 4 JS + 4 CSS + 3 shells; D9 on 3 subject surfaces); **live injection** proved it catches a real violation (appended `eke-subject` to `src/places/epr-vents.css` → exit 1 with the correct file/kind → reverted, `git` clean); `npm run build` green (`prebuild` runs the gate). Files: `scripts/cinematic-grammar.mjs`, `scripts/check-cinematic-grammar.js`, `scripts/check-cinematic-grammar.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M37 per protocol; next is M38 (D10, the composite grammar-rejection CI gate that consolidates this).
- **M35** (2026-07-04) — **Observatory v2, fourth roadmap milestone: in-place interrogate from inline validator JSON (D5).** Implements the **interrogate** primitive as a **depth-local reveal** on the evidential surface. Each claim in the evidence ledger (`scripts/build-evidence.mjs`) now carries a native **`<details class="interrogate">`** reveal: it opens **in place** (the `<details>` toggles inline — no `href`, no navigation, no depth change) and discloses the claim's **full evidence chain** — the **verbatim validator verdict** (`CONFORMANT` / `NON_CONFORMANT`), the **reason codes**, the **source**, and each occurrence's **backbone / as-of / pinned status** (occurrenceID, scientific name, GBIF-backbone reconciled?, eventDate, backbone-version pinned?). Everything is **inlined at build time straight from `check-bindings.js --json`**; the reveal is **static HTML with zero JS**, so **nothing is recomputed in the browser**. A new **pure, dependency-free `scripts/evidence-interrogate.mjs`** (`interrogationChain(record)`) only **selects and shapes** validator fields — it **never re-derives** evidentiary logic (single source of truth = the validator), unit-proven to copy every field verbatim (nulls preserved, non-string reason codes dropped, defensive on empty input). The occurrence-level detail moved from the always-visible summary **into** the reveal (the point of interrogate — depth on demand). **D3 upheld:** cinematic surfaces host **no** interrogate affordance (confirmed absent from every `dist/assets/places-*.js`; the reveal is depth-local to the evidential ledger, absent from the atlas/research bundles too). **Frozen architecture preserved:** no manifest/schema/validator change; L1 stays the build gate (`prebuild` unchanged); standards reuse only. **M34 not regressed:** the reach-not-truth badges and verbatim reason-code chips remain in the always-visible claim. **Nice consequence:** the verbatim verdict token now lives in the interrogation reveal — the raw-audit place it belongs — closing the M34-review observation that the literal verdict appeared nowhere, **without** reintroducing pass/fail language into the badge. **Verification:** new `scripts/evidence-interrogate.test.mjs` (21 checks: verbatim verdict/source/occurrence fields, reachCodes-then-L1-reasons, null-not-fabricated, defensive input), wired into `verify` (now 11 checks, green); `npm run build` green; **headless real-data cross-check** — parsed `check-bindings.js --json` (+ L2), asserted every ledger's `<details>` count equals its record count and **all 190 inlined validator fields matched exactly** across the four ledgers; `dist/evidence/epr-vents.html` ships 10 interrogate reveals. Files: `scripts/evidence-interrogate.mjs`, `scripts/build-evidence.mjs`, `scripts/evidence-interrogate.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M35 per protocol; the evidential-depth roadmap (M33 D7, M34 D6, M35 D5) is now complete, with M36 (D4) done in parallel — next is M37 (D3/D9 build gates).
- **M36** (2026-07-04) — **Observatory v2, third roadmap milestone: typed-edge *follow* in the atlas (D4).** Implements the **follow** primitive as an **RO-typed lateral edge at the same depth** on the analytical surface. The atlas field-record's interaction web — previously a flat list of relationship sentences — is now an **actor-grouped, RO-typed, laterally-followable graph**: each actor is an addressable node (`id="fr-node-<occurrenceID>"`); under it, each edge **names its OBO Relations Ontology relation linked to the controlled IRI** (`http://purl.obolibrary.org/obo/RO_…`) the validator checks (`check-bindings.js` `CONTROLLED_RELATION_IRI`), and lets you **follow** it to the actor it links via an in-page `#fragment` — **lateral, same depth**, never a press-in to the evidence ledger or a step-back to the cinematic surface. An edge whose `relationshipOfResourceID` is not a controlled IRI is marked **untyped rather than faked**. The whole web is **derived from `resource-relationship.txt` + the occurrence records** (`RELS`/`actorList`); nothing hand-authored. The pure model lives in a new **dependency-free `src/atlas/interaction-web.js`** (`interactionWebModel` + `isRoIri`), so the follow logic is unit-testable in Node and shared with the browser renderer; `src/atlas/field-record.js` only renders it (+ a small a11y focus / `:target` highlight pulse on follow, reduced-motion-safe). `src/atlas/atlas.js` (the discovery overview) was **not** touched — it renders no interaction edges. **Frozen architecture preserved:** no manifest, schema, or validator change; L1 stays the build gate (`prebuild` unchanged); **cinematic surfaces untouched** (confirmed: no follow-web logic in any `dist/assets/places-*.js`); standards reuse only (OBO RO PURLs, no new vocabulary). The follow is same-depth DOM navigation (the grammatically essential lateral act) and is deliberately **not** coupled to the canvas focus system (avoids fighting the scroll-driven narrative focus; not required by D4). **Verification:** new `scripts/interaction-web.test.mjs` (19 checks: RO-typing, verbatim IRI, lateral follow-targets, untyped-not-faked, derived-only, defensive input), wired into `verify` (now 10 checks, green); `npm run build` green; **real-data render check across all four archives** — sundarbans/coral-triangle/epr-vents 20 edges each, amazon-várzea 16, **every edge RO-typed, every IRI valid, every follow-target lateral**; built `dist/assets/field-record-*.js` carries the follow web, absent from every cinematic bundle. Files: `src/atlas/interaction-web.js`, `src/atlas/field-record.js`, `src/atlas/field-record.css`, `scripts/interaction-web.test.mjs`, `package.json`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M36 per protocol; M35 (D5, in-place interrogate) remains, then M37.
- **M34** (2026-07-04) — **Observatory v2, second roadmap milestone: verdict + reason-code "reach-not-truth" badges (D6).** Completes the badge grammar M33 began on the evidential surface. Before: only the `gap` state carried reason codes (`GAP: <codes>` smuggled into the badge), the `open` badge read "UNRESOLVED", and traceable read "TRACEABLE" — a mix that still leaned on defect/pass-fail framing. Now every claim carries a **uniform badge that names how far its warrant *reaches*, not whether it is true**: `REACHES EVIDENCE` (resolvable source), `REACHES A SOURCE` (named source, resolution open — the D7 first-class non-resolution state), `REACH INCOMPLETE` (baseline binding not yet established). **No pass/fail, "error", or check/cross language** appears in any badge — enforced by a unit test against a forbidden-term list. Beside each badge, the **validator's reason codes are shown verbatim** as monospace `<code>` chips (e.g. `SOURCE_UNRESOLVABLE`), taken straight from `check-bindings.js`'s append-only vocabulary via a new `reasonCodesVerbatim` helper that filters non-strings but never paraphrases, renames, or hides a code. Badges remain **derived from the validator** (`classifyReach` from the L1 verdict; codes = the L2 reason set passed through) — no re-derivation, so the page cannot drift from the verdicts. The `open` state keeps its calm D7 first-class note ("an open question… not a failure"), so **M33 is not regressed**. As a side consistency fix on the same surface, the ledger **index now names all three reach-states** (closing the M33-review residual where the index could not express `gap`). The full verdict string is deliberately *not* reprinted — it would reintroduce "non-conformant" pass/fail language D6 forbids; the reach badge + verbatim codes carry the exact machine signal without it. **Invariants preserved:** L1 stays the build gate (`prebuild` unchanged); evidence HTML derived/git-ignored; standards reuse only (no new reason code, no schema change); **cinematic surfaces untouched** (confirmed: no reach/badge logic in any `dist/assets/places-*.js`). **Verification:** `scripts/evidence-reach.test.mjs` extended with D6 assertions (three distinct reach-naming badges, forbidden-term guard, verbatim pass-through) — now 19 checks — and stays wired into `verify` (9 checks, green); `npm run build` green; regenerated `public/evidence/*.html`; `dist/evidence/epr-vents.html` confirmed to ship 10 `REACHES A SOURCE` badges + 10 verbatim `<code>SOURCE_UNRESOLVABLE</code>` lines + the D7 note. Files: `scripts/evidence-reach.mjs`, `scripts/build-evidence.mjs`, `scripts/evidence-reach.test.mjs`, `OBSERVATORY_V2_IMPLEMENTATION.md`, `PROJECT_STATUS.md`, session diary. One clean commit. Stopped after M34 per protocol; M35 (D5, in-place interrogate) is next.
- **M33** (2026-07-04) — **Observatory v2, first roadmap milestone: non-resolution rendering (D7).** Two parts. **(a) Canonicalization:** the frozen v2 design cascade (Interaction Grammar's one invariant / one axis / four primitives / traceability-not-truth terminal semantics; Architecture Decisions D1–D10; the M33–M40 roadmap) previously lived only in a design conversation; it is now transcribed verbatim into the repository as `OBSERVATORY_V2_IMPLEMENTATION.md` — the canonical roadmap **and** implementation ledger the session protocol references. No redesign; transcription only. **(b) M33 = D7:** the evidential surface now treats **non-resolution as a first-class terminal state**. The evidence ledger (`scripts/build-evidence.mjs`) previously rendered every binding as one of two states — `TRACEABLE` (green) or a red `GAP` — which reads as pass/fail and **over-claimed**: at baseline (L1) all 38 real bindings are conformant, so the ledger showed "38/38 traceable, all green" while concealing that **not one binding source resolves to a persistent identifier** (all 38 are `SOURCE_UNRESOLVABLE` at L2 — the `relationshipAccordingTo` fields are still citation strings, untouched curator work). A new **pure, unit-tested classifier** (`scripts/evidence-reach.mjs`, `classifyReach`) derives three reach-states **from the validator's own output** (no re-derivation → the page cannot drift from the verdicts): **traceable** (baseline-conformant + source resolves to a PID), **open** (baseline-conformant but source is a citation with no resolvable identifier — rendered calmly in slate with a per-claim italic note framing it as an open question, *not* a failure and *not* a gap), **gap** (baseline-nonconformant, e.g. `SOURCE_MISSING`; still names the unmet codes). `build-evidence.mjs` now runs the validator at L1 (authoritative verdict) and L2 (the `SOURCE_UNRESOLVABLE` signal), aligns the two record arrays positionally, and classifies each claim; headlines + index are honest three-state summaries. On the real corpus every claim renders **open** — a truthful mirror of the DOI-unconfirmed backlog instead of a false wall of green. **Invariants preserved:** L1 stays the build gate (`prebuild` unchanged); evidence HTML stays derived/git-ignored; standards-reuse only (no new reason code, no schema change); the "traceability, not truth" framing intact; cinematic surfaces untouched. **Verification:** new `scripts/evidence-reach.test.mjs` (`npm run test:evidence-reach`, 11 classifications incl. all three states) wired into `verify` (now 9 checks); `npm run verify` and `npm run build` both green; confirmed `dist/evidence/*.html` ship the three-state rendering (10 per-claim reach notes + header note in the built EPR ledger). Files: new `scripts/evidence-reach.mjs`, `scripts/evidence-reach.test.mjs`, `OBSERVATORY_V2_IMPLEMENTATION.md`; modified `scripts/build-evidence.mjs`, `package.json`. Stopped after M33 per protocol; M34 (D6, reach-not-truth badges) is next.
- **M32** (2026-07-04) — Continuation of the EPR DwC-A reference audit: the sole remaining thread, the Bates et al. (2005) citation ambiguity (backlog item 4), was re-examined rather than left as-is. Method matched M31's rigor: resolved each candidate DOI directly against `api.crossref.org/works/<doi>` rather than trusting a single search summary. Confirmed (again, independently) that the 2005 candidate (Bates, Tunnicliffe & Lee, *Marine Ecology Progress Series* 305:1–15, `10.3354/meps305001`, "Role of thermal conditions in habitat selection by hydrothermal vent gastropods") matches the cited year and multi-author byline but its abstract concerns thermal-microhabitat partitioning, not grazing/diet; the 2007 candidate (Bates, *Marine Ecology Progress Series* 347:87–99, `10.3354/meps07020`, "Feeding strategy, morphological specialisation and presence of bacterial episymbionts in lepetodrilid gastropods from hydrothermal vents") matches the grazing/feeding-strategy subject but is sole-authored (no "et al.") and dated two years off. **New finding this session:** a 2006 University of Victoria PhD dissertation by the same author, "Population and feeding characteristics of hydrothermal vent gastropods along environmental gradients with a focus on a bacterial symbiosis hosted by *Lepetodrilus fucensis*," spans both the grazing/feeding and microhabitat/environmental-gradient themes at once — raising a real possibility that the archive's citation informally synthesizes Bates's PhD-era research program rather than naming one indexed, resolvable paper. No persistent identifier for the thesis was found or asserted. **Outcome: still deliberately unresolved** — neither candidate cleanly fits both the byline and the subject, and forcing either would violate the project's own rule that a wrong DOI is worse than a citation string. `public/dwca/epr-vents/CREDITS.md`'s Bates entry and its "DOI status" summary paragraph were rewritten to document the fuller picture (three candidates now named, not two) for the next Research Curator; `PROJECT_STATUS.md` backlog item 4 updated to match. No other citations re-examined (the handoff explicitly marked the other 7 as independently verified and not needing re-search). No code, schema, or architecture touched — content-only change to a `public/` data file. `npm run verify` (8/8) and `npm run build` both green; `diff` confirms `CREDITS.md` ships byte-identical into `dist/`.
- **M31** (2026-07-03) — EPR DwC-A reference audit (backlog item 1, the M9C-equivalent validation step CREDITS.md had flagged since M19). Instructed to prioritize user-facing scientific value over further repository infrastructure, so this session used live web lookups (CrossRef) instead of adding tooling. All 9 non-1981 "[DOI unverified]" citations in `public/dwca/epr-vents/CREDITS.md` were checked; **7 confirmed** (Lutz 1994 `10.1038/371663a0`, Desbruyères 1998 `10.1016/S0967-0645(97)00083-0`, Micheli 2002 `10.1890/0012-9615(2002)072[0365:PSCADS]2.0.CO;2`, Sancho 2005 `10.1016/j.dsr.2004.12.002`, Van Dover 2018 `10.1016/j.marpol.2018.01.020`, Boschen 2013 `10.1016/j.ocecoaman.2013.07.005`, Robidart 2008 `10.1111/j.1462-2920.2007.01496.x`) — every DOI independently re-verified by resolving `api.crossref.org/works/<doi>` directly and checking the returned title/authors/volume/pages against the citation, not accepted from a single search summary. Robidart's title was also corrected (the archive had paraphrased it; the real title is "...revealed through metagenomics"), the same class of fix M9C/M11/M13 made for other archives. **One citation deliberately left unresolved rather than guessed:** Bates et al. (2005) "Grazing and microhabitat of the vent limpet *Lepetodrilus*" — the closest 2005 match by the same author is about thermal habitat selection, not grazing, and the paper that actually matches "grazing" is dated 2007; rather than force an uncertain match (this project's own rule: "a wrong DOI is worse than a citation string"), the ambiguity is documented in `CREDITS.md` and added to the backlog as item 4 for a Research Curator to resolve by reading the actual papers. Van Dover (2000) remains without a DOI for the correct reason (it's a book). **This is genuinely user-facing**: `CREDITS.md` is linked directly from the live `atlas/epr-vents.html` page (`src/atlas/field-record.js`), so a visitor can now click through to 7 more real, resolving papers instead of bare citation strings. Scope discipline: only `CREDITS.md` (the human-readable literature list) was touched; `public/dwca/epr-vents/resource-relationship.txt`'s `relationshipAccordingTo` fields (the L2 `SOURCE_UNRESOLVABLE` upgrade — multi-citation fields needing a design decision about representing several sources as one resolvable identifier) were deliberately left alone as separate, larger curator work already scoped in `.agents/HANDOFF-eke-completion.md` item 2. No code, schema, or architecture change; `npm run verify` (8/8) and `npm run build` both green (unaffected, as expected for a content-only `public/` file); confirmed the corrected `CREDITS.md` ships byte-identical into `dist/dwca/epr-vents/CREDITS.md`.
- **M30** (2026-07-03) — DwC-A XML well-formedness gate. New `scripts/check-dwca-xml.js` (`npm run check-dwca-xml`) scans every `public/dwca/<slug>/{meta.xml,eml.xml}` with a dependency-free, stack-based well-formedness scanner (Node stdlib only, matching the `check-narratives.js`/`check-manifest.js`/`check-bindings.js` pattern): mismatched/unclosed tags, wrong root-element count, and unescaped bare `&` (quote-aware, so `>`/`&` inside quoted attribute values are handled correctly; comments/CDATA/declarations are skipped as opaque spans). **Closes a live, previously-undetected gap**, not a hypothetical one: `check-manifest.js` only ever checked these files *exist*; `scripts/ingest/build-dwca.mjs`'s `eml()` template interpolates `place.name`/`place.locality` straight into XML text content with zero escaping, so a future place name containing `&`, `<`, or `>` would have shipped a malformed `eml.xml` undetected — and these exact files are linked as direct visitor downloads from the atlas field-record page (`src/atlas/field-record.js`) and are the artifacts external biodiversity-informatics tools (GBIF/GloBI) would ingest. All 4 archives' 8 XML descriptors currently pass. New unit test `scripts/check-dwca-xml.test.mjs` (`npm run test:dwca-xml`) negative-tests every claimed failure mode (unclosed tag, mismatched close, wrong root count, bare `&` in text and in an attribute value, orphan close tag) plus realistic well-formed inputs (comments/CDATA/entities/`>` inside a quoted attribute) — a check that only ever passes proves nothing. Live-verified beyond the fixtures: injected a real unescaped `&` into `public/dwca/sundarbans/eml.xml`, confirmed the checker caught it (exit 1, correct line number), then restored the file (`git diff` clean). **Graduated straight into `prebuild`** (a build invariant, alongside `check-bindings`) rather than starting in `verify`-only, because these exact files are copied into every `dist/` and linked as user-facing downloads — the same reasoning that graduated `check-bindings`. `npm run verify` (now 8 checks, both new checks included) and `npm run build` (prebuild runs `check-dwca-xml` before `build:evidence`) both green. Closes the "EML lint" component of backlog item 3 (the roadmap's Zenodo/GBIF/Software-Heritage items remain deliberately deferred).
- **M29** (2026-07-03) — EPR atlas backdrop biome (backlog item, deferred at M20). `src/prototypes/biome-backdrop.js` — consumed in production by `src/atlas/field-record.js` (`atlas/epr-vents.html`) — gained an `abyssal` recipe: no daylight `sky.sun` highlight (set equal to `sky.top` so the shared engine's fixed near-top radial glow position, correct for daylight biomes, does not misplace a false glow in a lightless scene), `water.y: 0` (already fully submerged, mirroring the existing `reef` pattern — no water-plane compositing), sparse dark bands (Article VI, darkness is content), and a new `smokers` foreground signature (vent chimneys: dark mineral column, warm thermal-glow base, pale mineral plume) reusing the exact palette already vetted on the cinematic EPR surface (`src/places/epr-vents.js` `PAL.ventCore`/`PAL.plume`) — no new color invented. `keyFor` gained a `/vent|hydrothermal|abyssal/` branch so `PLACE_META.type` ("Hydrothermal vent field") routes to it instead of falling through to `default`. **Fixes a scientific-communication mismatch**, not a cosmetic one: the atlas field record for a lightless, chemosynthetic hydrothermal vent field was illustrated with a sunlit painterly marsh backdrop. Strictly additive — the other four recipes (mangrove/várzea/reef/savanna) and `default` are untouched; the cinematic EPR surface (a separate, bespoke module) is untouched. New unit test `scripts/biome-backdrop.test.mjs` (`npm run test:biome-backdrop`, wired into `verify`) asserts `keyFor` routes every place type correctly, including the vent field and unrecognised/empty types; dependency-free, no DOM. Verified in the production build: `abyssal`/`smokers`/the vent regex all ship in `dist/assets/field-record-*.js`; absent from every `dist/assets/places-*.js` (cinematic purity intact, confirmed by grep). `npm run verify` (now 6 checks) and `npm run build` both green.
- **M28** (2026-07-03) — Embodiment Phase, founding increment. Recorded the founding specification (`.agents/decisions/2026-07-03-eke-reference-implementation-founding-spec.md`) that reframes the three surfaces as one **Ecological Knowledge Environment** and names its durable core — a **provenance-binding conformance profile + validator** certifying **traceability, not truth** — built by reuse of existing standards (Darwin Core ResourceRelationship, OBO Relations Ontology, GBIF backbone, PROV/ECO), with the six governing pillars as the standing evaluation rubric, explicit boundaries (never truth / never a new vocabulary, format, or authority / presentation stays permanently experimental / CARE overrides traceability), and the permanently-retired novelty claims (new discipline/law/construct/instrument/ontology). Shipped the reference validator's **first behavior**: `scripts/check-bindings.js` (+ `npm run check-bindings`) — a dependency-free, manifest-driven, claim-level traceability check over the four archives with an **append-only reason-code vocabulary** (`EVIDENCE_UNRESOLVABLE`, `RELATION_UNTYPED`, `SOURCE_MISSING`, `BACKBONE_UNRECONCILED`, `AS_OF_MISSING`) and two on-spec corpus overrides (`EKE_MANIFEST`, `EKE_DWCA_ROOT`) so it can run against a conformance test corpus. Generalizes the count-parity seed already in `check-manifest.js`. **All 38 interaction bindings across the 4 archives certify traceability-conformant;** discrimination proven against a deliberately-broken fixture (1/6 conformant, each of the five reason codes fires on its intended row, exit 1). Additive only; `check-narratives` (13) + `check-manifest` (4) still pass. Initially kept out of `prebuild` while the profile matured (now graduated — see below). **Reference-implementation increment (same session):** the validator now emits machine-readable per-binding verdicts (`--json` / `EKE_JSON`), and a **versioned conformance corpus** (`test/conformance/`: `clean` + `mixed` fixtures, a `build.mjs` generator, and an `expected.json` contract) plus a **black-box harness** (`scripts/check-bindings.test.mjs`, `npm run test:conformance`) now *define* conformance as verdict-and-reason agreement over the corpus — the mechanism that makes this a reference implementation any independent implementation can be checked against. Harness passes (8 bindings match the contract; the `mixed` archive exercises all five reason codes). **The reference-implementation triad is now complete in kernel form:** the human-readable **profile specification** (`docs/interaction-claim-binding-profile.md`) maps every primitive onto existing Darwin Core / OBO RO / GBIF / ECO-PROV terms (reuse-only, invents nothing), states the append-only reason-code vocabulary, defines additive conformance levels (L1 baseline traceability — satisfied by all 4 archives — through L3 pinned-backbone + typed-evidence), and binds the CARE constraint as non-negotiable. Specification + reference implementation (`check-bindings.js`) + conformance corpus (`test/conformance/`) together let any independent implementation be checked for conformance without sharing code. Scope: interaction claims only. **Conformance level L2 implemented (opt-in, `--level=L2`):** an additive `SOURCE_UNRESOLVABLE` code requires interaction-claim sources to resolve to a persistent identifier (DOI or URL), not a citation string; the corpus now carries per-level (L1+L2) expectations and the harness asserts both (16 binding-checks). L1 stays the default gate (real corpus 38/38, `npm run verify` green); at L2 the real corpus reports 38 `SOURCE_UNRESOLVABLE` — a precise, actionable rigor roadmap that independently rediscovered the backlog's DOI-unconfirmed EPR-vents citations. Added `npm run verify` (runs all four checks in one command). A `--level=L9`-style unknown level is rejected. **Adoption affordance:** the validator now also accepts Darwin Core archive directories as positional arguments (`node scripts/check-bindings.js <archiveDir>...`), so any external DwC-A producer (e.g. GloBI contributors) can validate their **own** interaction bindings against the profile without adopting the Place Manifest — manifest-driven discovery remains the default with no arguments. **Conformance level L3 (partial, opt-in `--level=L3`)** with a governance-respecting data-model decision record (`.agents/decisions/2026-07-03-l3-conformance-data-model.md`): two additive checks that are pure Darwin Core reuse — `BACKBONE_UNPINNED` (occurrence declares `dynamicProperties.backboneVersion`) and `ASSERTION_UNATTRIBUTED` (`relationshipEstablishedDate` populated) — are enforced; a third, `EVIDENCE_CODE_MISSING` (an ECO/SEPIO evidence code), is **reserved but deliberately not implemented** because Darwin Core has no field for it and inventing one locally would violate standards-over-invention — its placement is routed to the Research Curator / TDWG. Corpus now carries per-level (L1/L2/L3) expectations; harness asserts all three (24 binding-checks). L1 remains the default gate (real corpus 38/38, `verify` green); at L3 all 38 real bindings report the upgrade path. No curated archive was modified. **Graduation:** now that the profile has matured (three levels + corpus + harness), `check-bindings` (L1) is wired into `prebuild` — evidence-traceability is now a **build invariant** (no build ships with an untraceable interaction claim; `prebuild` green); higher levels remain opt-in diagnostics. **Evidence-chain output (first embodiment of the environment's evidential depth, in a verifiable non-UI medium):** `--trace` renders, per interaction claim, the full audit trail (subject `--relation-->` object, source, and each evidence occurrence's backbone/date/pin status + verdict + reason codes); the `--json` records now carry the same evidence chain (`source`, `subject`, `object`), so a future surface or an AI system consumes the provenance without re-deriving it. This is the "trace any claim to its evidence and see how far it reaches" capability as a usable tool today. **First real surface of the unified environment — the evidential depth:** `scripts/build-evidence.mjs` (`npm run build:evidence`, wired into `prebuild`) generates a static, self-contained HTML **evidence ledger** per place (`public/evidence/<slug>.html` + index) directly from the reference validator's output, so a page can never drift from its verdicts. Pure **evidence before spectacle** — the light editorial record register (Georgia/Iowan Old Style, warm paper), no animation — so it renders and is fully verifiable headlessly (well-formed HTML, all 38 claims shown with relation + source + traceability badge). Derived and git-ignored (regenerated every build; not a source of truth, no dual-source). This is the deepest, most substrate-independent stratum of the environment (founding spec §3); the experiential/analytical surfaces layer on top of it later without re-deriving the evidence. **Cross-depth traversal (the unification made real, verifiably):** each ledger now navigates *up* the epistemic-depth axis — to the analytical (atlas interaction web), experiential (cinematic encounter), and research (field notes) depths — via the manifest's own cross-surface bindings (ADR-001), so the mapping is correct where slugs differ (Coral Triangle's ledger links to `places/crossing.html`) and absent where a surface is (Amazon várzea has no cinematic link). All cross-depth links resolve to existing surfaces (no dangling links); it is the step-back/ascend grammar embodied as plain, verifiable navigation — no animation. **Reciprocal descent built + verified (bidirectional research ↔ evidential):** the pure cross-surface derivation was extracted to `src/notes/surface-links.js` (unit-tested in Node, `npm run test:surface-links`, wired into `verify`) and now also emits an `Evidence ledger →` **descent** link (manifest-driven: present only where a place has a DwC archive). Verified headlessly three ways — the derivation unit test passes, the full `npm run build` succeeds, and the shipped bundle (`dist/assets/render-narrative-*.js`) contains the `../evidence/` descent link — so it is real in production, not just asserted; `dist/evidence/*.html` ships the ledgers. **Atlas → evidential descent added (and cinematic purity upheld):** the atlas field-record nav (`src/atlas/field-record.js`, manifest-derived per M25) now also appends the `Evidence ledger →` descent link where the place has a DwC archive; the **cinematic surface deliberately receives none** — the one-way-bridge doctrine (no cinematic exit affordance) is preserved, verified by grep against the shipped `places-*.js` bundles. Confirmed in the production build: the atlas descent ships in `dist/assets/field-record-*.js`, and no cinematic bundle links to the ledger. **Cross-depth navigation is now complete for every doctrine-permitted direction** — research→evidential and atlas→evidential (descent, both in shipped bundles), evidential→research/analytical/experiential (ascent, on the ledger pages), cinematic pure by design. **Transition grammar — standards baseline wired:** the "cut hidden in a dip" between depths is now implemented via the **W3C View Transitions API** (cross-document `@view-transition{navigation:auto}` + a calm ~350ms root cross-fade), added to the three non-cinematic surface stylesheets (research, atlas overview, atlas field-record) and the generated evidence ledgers. It degrades to plain multi-page navigation where unsupported (stays fully deployable as an ordinary website — progressive embodiment) and is pure evidence-before-spectacle (a subtle fade, not spectacle). **Cinematic purity upheld:** the opt-in is absent from all `places-*.css` — verified by grep against the built `dist/assets/*.css` (present in exactly render-narrative + atlas-index + field-record CSS, and the evidence HTML; absent from cinematic). The **subject-through-line morph is now wired across both descent paths**: the place title carries a shared `view-transition-name: eke-subject` on the research page (`#narrative header h1`), the atlas field-record masthead (`.fr-masthead h1`), and every evidence ledger (`.wrap h1`), so the title morphs across **research→evidential and atlas→evidential** descents, degrading to the root cross-fade and then to plain navigation. Verified paired in the production build (the name ships in `dist/assets/render-narrative-*.css` and every `dist/evidence/*.html`; absent from all `places-*.css` — cinematic purity intact). Only final easing/timing *polish* now needs a browser to confirm visually — the transition grammar's mechanism (dip + subject morph) is complete and shipping. **Completion handoff:** `.agents/HANDOFF-eke-completion.md` is a verified, actionable checklist for the four items an autonomous headless session cannot supply — final transition easing (a browser, ~30 min), curated-archive L2/L3 rigor (Research Curator, per-archive verified DOIs/backbone-versions/dates — never invented), the evidence-code field (TDWG placement), and adoption (contribute up to GloBI, Zenodo release, outreach; enablers already built). Every reference and command in the handoff was verified to resolve/run. None require re-architecture. **Adoption + durability enablers:** the top-level `README.md` gained an adopter on-ramp ("Evidence-preserving communication — the reference implementation") letting any external DwC-A producer validate their own archive in one command (verified: every documented command runs, every link resolves); and a new CI workflow `.github/workflows/verify.yml` runs the **full evidentiary gate** (`npm run verify` — checks + conformance harness + unit tests) on every push and pull request, so the conformance definition cannot silently regress (build/deploy already enforced L1 via `prebuild`; this adds the harness + tests build alone does not run).
- **M27** (2026-07-02) — Retired `public/dwca/index.json`: the Place Manifest is now the **single canonical source of truth** (resolves the last dual-source debt from the v3.0 certification). Approach: consume the manifest directly (no generated file, no build codegen, no runtime fetch). `src/atlas/atlas.js` builds the field-records discovery panel from `PLACES` (places with a dwca + field-record atlas); `src/atlas/field-record.js` reads `PLACE_META` (name/type) via `getPlaceBySurfaceSlug(PLACE)`; `scripts/ingest/build-dwca.mjs` no longer emits `index.json` (would resurrect the dual source); the manifest `places` array was reordered (clean block move) so discovery renders in byte-identical order to the retired file; `atlas.css` stale comment updated; `public/dwca/index.json` deleted. Discovery panel and field-record metadata proven byte-identical (order + content); no production consumer references the file (only dev-only prototypes, excluded from the build); `index.json` absent from repo + dist. `check-narratives` (13) + `check-manifest` (4) pass; build green. **ADR-001's single-source-of-truth goal is now total.**
- **M26** (2026-07-02) — Place Manifest Phase 1C: the **Homepage** consumes the manifest (final consumer; ADR-002 hybrid). `src/main.js` replaces the three bespoke arrival functions with one `arrive(place, captionEl)` dispatcher keyed on `cinematic.arrival.kind` (globe-hotspot vs dip); `setupCaptions()` resolves each static caption to its place via the research-note slug in its href. Timings/easings/camera-fly/luminance-dip/cut targets are byte-for-byte the prior grammar — only the data source is the manifest. **Curation preserved:** captions remain human-authored static anchors in `index.html` (unchanged); nothing generated, no auto-inclusion. Arrival parity proven for all three curated places; both timelines' constants verified; `index.html` untouched; 0 bespoke arrivals / 0 stale `pageCaption` refs. `check-narratives` (13) + `check-manifest` (4) pass; build green (+87/−247). **The ADR-001 consumer migration is complete** — Research (M24), Atlas (M25), Homepage (M26) all read the manifest; Cinematic surfaces stay pure.
- **M25** (2026-07-02) — Place Manifest Phase 1B: the **Atlas surface** consumes the manifest. `src/atlas/atlas.js` drops the 5 `n.place.id ===` detail-card branches (bridge links derived via `getPlaceByNarrativeId(n.id)`); `src/atlas/field-record.js` drops the sundarbans/coral-triangle/epr-vents nav branches (back-nav derived via `getPlaceBySurfaceSlug(PLACE)` — companion atlas links then the cinematic enter link; "← Living Atlas" still first). Derivation order matches M24. The two remaining `PLACE === 'sundarbans'` uses in field-record.js are bespoke **content** (Sundarbans steps override + sundarbans-only foot note), not navigation, and are intentionally preserved. Output parity proven for both consumers across all places (old branches vs derivation = byte-identical link lists); URLs and atlas rendering unchanged. `check-narratives` (13) + `check-manifest` (4) pass; build green. Homepage (main.js) and Cinematic untouched. Next: M26 Phase 1C (Homepage) — the last consumer.
- **M24** (2026-07-02) — Place Manifest Phase 1A: the **Research surface** consumes the manifest. `src/notes/render-narrative.js` drops the per-place `SURFACE_LINKS` table and derives cross-surface links from the manifest via `getPlaceByNarrativeId(n.id)` (field-record atlas → "Interaction web →", companion atlas → "Research companion →", cinematic → manifest `enterLabel`; research-only narratives → none). Output parity proven for all 4 places + a research-only narrative (old table vs derivation = byte-identical link lists; HTML wrapper unchanged); URLs preserved; manifest now bundled into the research bundle only. Atlas (5 branches), Homepage (main.js arrivals), and Cinematic left untouched — deferred to later phases. `check-narratives` (13) + `check-manifest` (4) pass; build green. Next: M25 Phase 1B (Atlas consumers).
- **M23** (2026-07-02) — Place Manifest, Phase 0 "describe, don't consume" (ADR-001; canonical representation = JSON source per the M23-preceding decision). `cinematic-language/place-manifest.json` is the single source of truth for cross-surface place bindings, describing the current state of the 4 multi-surface places (sundarbans; coral-triangle with cinematic slug `crossing` + companion atlas; east-pacific-rise as `epr-vents`; amazon-varzea with no cinematic/homepage); research-only narratives excluded by convention. Added `place-manifest.schema.json` (JSON Schema draft 2020-12), `place-manifest.ts` (typed wrapper, unconsumed → unbundled), and `scripts/check-manifest.js` (dependency-free validator: schema-shape, canonical ids, referenced-file existence, DwC-A count parity, navigation bindings), wired into `prebuild`. No consumer reads the manifest; no runtime, navigation, or existing-code change (confirmed: `place-manifest` absent from `dist/`). Negative-tested (catches wrong dwca count + missing hotspotId). Build green. Next: M24 Phase 1a (research + atlas nav consume the manifest).
- **M22** (2026-07-02) — Before-PhD open-science governance foundation (approved Option-B strategy; decision papers 2026-07-02). Content-typed licensing: `LICENSE` (Apache-2.0, code) + `LICENSING.md` map; documentation, editorial prose, and Darwin Core data under CC-BY-4.0; NC/ND/copyleft excluded. `CITATION.cff` (CFF 1.2.0 software citation; version 2.0.0; author handle flagged for full-name/ORCID before first formal citation). `THIRD-PARTY-NOTICES.md` (GBIF backbone, GloBI, OBO RO, three.js/GSAP/Lenis/Vite; media policy). `SENSITIVE-DATA-POLICY.md` (coordinate-generalisation safeguard per GBIF sensitive-species guidance). All four `eml.xml` gained `<intellectualRights>` (CC-BY-4.0) in EML 2.1.1 order; `build-dwca.mjs` template updated so regeneration preserves rights. `package.json` gained license/description/author/repository/homepage. No architecture, functionality, or DwC-A structural change (metadata only). Validated: EMLs well-formed with rights; JSON/YAML valid; build green; rights present in all `dist/` archives. Zenodo/DOI, codemeta, schema.org, CI, Software Heritage, and GloBI deferred (backlog 4).
- **M21** (2026-07-02) — East Pacific Rise homepage integration (first post-v2.0 milestone). Closes the sole Major warning from the v2.0 Independent Release Review: EPR had no entry point on the planetary homepage. Added a third caption `#page-caption-epr` ("East Pacific Rise · Giant tubeworm") in `index.html` with a research-note href fallback, wired in `src/main.js` to `arriveToEPR()` — a verbatim mirror of `arriveToCrossing()` (no globe hotspot → luminance-dip-only Article III arrival into `places/epr-vents.html`; dark-to-dark continuity) — and revealed with the other captions in the landing sequence. `src/style.css` adds `.page-caption--tertiary`, stacking the third caption above the secondary in the same subordinate ink-low register (desktop + mobile). Reuses the existing caption pattern; no homepage redesign, no globe/hotspot change, no changes to Cinematic/Atlas/Research implementations. All three cinematic places now have homepage parity. Build green; `#page-caption-epr` verified in `dist/index.html` and the arrival target in the bundle.
- **M20** (2026-07-02) — East Pacific Rise atlas surface: `atlas/epr-vents.html` (thin shell mirroring `amazon-varzea.html`), rendering the M19 DwC-A live through the data-driven `field-record.js` engine. Archive registered in `public/dwca/index.json` within the same milestone so the atlas discovery chip resolves (M19 deferral closed). Navigation wired into the three existing per-place structures — research→atlas (`render-narrative.js` SURFACE_LINKS), atlas-overview detail card and atlas field-record nav (`atlas.js`, `field-record.js`) — preserving the Cinematic↔Atlas↔Research pattern; cinematic surface untouched. Auto-discovered as a vite build input; emitted to `dist/atlas/`. No atlas-architecture change, no new infrastructure. EPR three-surface experience now complete. Build green. Deferred: a vent-specific backdrop recipe (backlog 3). Diary: `.agents/sessions/2026-07-02-m20-epr-atlas.md`.
- **M19** (2026-07-02) — East Pacific Rise DwC-A designed and assembled (Research Curator): `public/dwca/epr-vents/` (occurrence.txt, resource-relationship.txt, meta.xml, eml.xml, CREDITS.md). 9 occurrences / 10 OBO-RO-typed interactions — the Observatory's only chemosynthetic (non-photosynthetic) trophic web: focal *Riftia pachyptila*, its *Candidatus* Endoriftia persephone endosymbiont, free-living *Beggiatoa* mat, vent mussel, Pompeii worm, limpet, crab, eelpout, *Homo sapiens*. GBIF backbone verified per-taxon; jurisdiction (ABNJ/ISA, empty countryCode), observational access (HumanObservation submersible + MaterialSample microbes), and abiotic vent chemistry (habitat + dynamicProperties.energySource, not a fake taxon) all ruled in `.agents/decisions/2026-07-02-epr-dwca-design.md`. Resolves the M15 open challenges. Not registered in `public/dwca/index.json` (bundled with the atlas build to avoid a chip to a missing page). Validated (columns, referential integrity, JSON, XML); build green.
- **M18** (2026-07-02) — Creative Director + UX review of all three cinematic surfaces (Sundarbans, The Crossing, East Pacific Rise) audited together as one Observatory. One Critical issue fixed across all three: render/ambient loops never paused when the tab was hidden (Principle XVII, codified non-negotiable). Added `visibilitychange` pause + a ~400ms resumption Hold with time-base reset in `src/places/crossing.js`, `epr-vents.js`, and `sundarbans.js`. Important/Nice-to-have findings recorded in the session diary, not implemented (per task scope): the Sundarbans↔canvas interaction-model divergence (architectural), the Sundarbans vignette vs Article V, cross-surface type-stack drift (Iowan Old Style vs Georgia), EPR camera `ay` framing vs The Crossing, screen-reader exposure of captions, and EPR/Crossing performance measurement. Build green.
- **M17** (2026-06-27) — Creative Director UX review of EPR cinematic surface. Three critical fixes in `src/places/epr-vents.js`: (1) warmth-beat/ventA misalignment — moved ventA gate from p=0.55 to p=0.45, so thermal glow is faintly present when beat 4 "a warmth with no source above it" lands at p=0.52; (2) compositing order — vent particles moved before worm tubes so atmospheric particles don't float in front of inhabitants (Article XVII); (3) depth gradient extended from `smoothstep(0,0.38)` to `smoothstep(0.05,0.60)`, maintaining visual texture in mid-descent; luminance dip repositioned to p=0.40 with narrower sigma. Full review in session diary.
- **M16** (2026-06-27) — East Pacific Rise cinematic surface live: `places/epr-vents.html`. Canvas 2D scroll-driven descent from sunlit surface through absolute abyss to vent field; 4 vents, 7 editorial beats, EPR biome palette (thermal orange, white smoker mineral, arterial-red worm plume, marine snow). Article XVII atmospheric hierarchy (thermal glow → plumes → worms). Article III luminance dip at p≈0.48. `SURFACE_LINKS` wired for research-to-cinematic nav. 3 files created, 1 modified. Build green.
- **M15** (2026-06-27) — Chief Architect ruling: East Pacific Rise hydrothermal vents selected as third cinematic place. 5-candidate evaluation (EPR, Amazon várzea, Santa Barbara Channel, Dinaric olm, Pando). EPR wins on cinematic potential and geographic diversity; Amazon várzea deferred (strong fallback). Decision record: `.agents/decisions/2026-06-27-third-cinematic-place-epr.md`. No files modified; decision only.
- **M14** (2026-06-27) — Track A: `amazon-varzea-arapaima-flood-pulse` promoted draft → verified. Castello (2008) DOI confirmed (`10.1111/j.1600-0633.2007.00255.x`); title corrected to match publication. Castello et al. (2009) JAI supplement not indexed in CrossRef — no DOI assigned, citation retained. `notes/index.html` updated. **13/13 verified.**
- **M13** (2026-06-27) — Track A: `hudson-river-tomcod-pcb-resistance` promoted draft → verified. Correction: year start 1989 → 2006 (Wirgin 1989 is K-ras/tumour biology; Yuan 2006 EHP is earliest PCB-resistance source). DOI-anchored (Wirgin 2011, Yuan 2006). `notes/index.html` updated. 12 verified.
- **M12** (2026-06-27) — Track A: `east-pacific-rise-tubeworm-chemosynthesis` promoted draft → verified. Correction: year end 2011 → 1991 (Childress et al. 1991 is latest source). Dual DOI-anchored 1981 Science sources confirmed. `notes/index.html` updated. 11 verified.
- **M11** (2026-06-27) — Track A: `dinaric-olm-century-lifespan` promoted draft → verified. Correction: `order` Urodela → Caudata (GBIF backbone). DOI-anchored (Voituron et al. 2011). `notes/index.html` updated. 10 verified.
- **M10** (2026-06-27) — Amazon várzea narrative: `amazon-varzea-arapaima-flood-pulse` (Arapaima gigas, draft). Notes shell + narrative `.ts` created; SURFACE_LINKS and atlas.js card updated. DwC-A orphan resolved; full 3-surface experience live. 13 narratives registered.
- **R1** (2026-06-27) — Release Patch R1: resolved two Independent Release Review blockers. Added "Interaction web →" to coral-triangle notes-surface SURFACE_LINKS (`src/notes/render-narrative.js`); added Hughes et al. (2007) to `public/dwca/coral-triangle/CREDITS.md`. Coral Triangle DwC-A certified as Observatory reference implementation.
- **M9C** (2026-06-27) — Scientific Validation Audit: GBIF key 5220228 corrected to 8841716 (*E. imbricata*); OCC:1 class/order corrected to backbone; two DOI errors resolved (Barott: correct DOI confirmed; Hoey & Bellwood: DOI removed); month "0" → empty.
- **M9B** (2026-06-27) — Coral Triangle field record: 9-actor DwC-A, 10 interactions, `atlas/coral-triangle.html`, atlas card + field-record nav.
- **M9A** (2026-06-27) — Coral Triangle ecological design (Research Curator); 8-issue Scientific Review + M9A Revision 1; spec at `.agents/decisions/2026-06-27-coral-triangle-design-v2.md`.
- **M1–M8** — see prior `git log` entries.
